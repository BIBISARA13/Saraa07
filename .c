#include <stdio.h>
int main(){ 
   int n,{1=0,12=1, nextterm;
printf("enter the number of terms:");
scanf("%d",&n);
printf(fibonacci series %d,%d",t1, t2);
for (int i=3;i<=n;i++){
next term =t1+t2
printf(",%d", next term):
t1=t2
t2 = next term
}

return 0;
}
